# add_to_dynamic_array
Example code (C++) for students of Bridge To Tandon

Take a look at why 'int*&' is used when passing in the array and what happens if instead 'int* array' is passed as a parameter.
